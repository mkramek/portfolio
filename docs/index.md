---
title: CV + Portfolio — Docs
---

Documentation for the CV + Portfolio + Admin Next.js application, split into two parts:

- **[[arch/00-overview|Architecture]]** — the durable reference: what the system is, why it's built this way, and the decisions behind it (including [[arch/adr/001-database-postgres|ADR]] records for the choices that had real alternatives).
- **Implementation** — phased, actionable build plans for implementation agents, each linking back to the architecture doc(s) it implements. Start at `impl/00-project-setup.md`.

Start with [[arch/00-overview]] if you're new here.
